#include <stdio.h>
/**
  * main - Prints the numbers from 0 to 9
  *
  * Return: Always (Success)
  */
int main(void)
{
	int number = 0;

	while (number < 10
	{
		putchar(number + '0');
		number++;
	}
	putchar('\n');
	return (0);
}
